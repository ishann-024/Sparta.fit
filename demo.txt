Still the button does not does anything
<div class="form-container">
    <h2>Candidate Registration</h2>

    <!-- Success Message -->
    <div *ngIf="successMessage" class="message success-message" style="display: block;">
        {{ successMessage }}
    </div>

    <!-- Error Message -->
    <div *ngIf="errorMessage" class="message error-message" style="display: block;">
        {{ errorMessage }}
    </div>

    <form #registrationForm="ngForm" (ngSubmit)="onSubmit($event)">
        <div class="input-group">
            <label for="fullName">Full Name</label>
            <input 
                type="text" 
                id="fullName" 
                name="fullName" 
                [(ngModel)]="candidateRegistrationDto.fullName"
                placeholder="Enter your full name" 
                required
                #fullName="ngModel">
        </div>

        <div class="input-group">
            <label for="email">Email Address</label>
            <input 
                type="email" 
                id="email" 
                name="email" 
                [(ngModel)]="candidateRegistrationDto.email"
                placeholder="Enter your email address" 
                required
                email
                #email="ngModel">
        </div>

        <div class="input-group">
            <label for="passwordHash">Password</label>
            <input 
                type="password" 
                id="passwordHash" 
                name="passwordHash" 
                [(ngModel)]="candidateRegistrationDto.passwordHash"
                placeholder="Create a strong password" 
                required
                minlength="6"
                #passwordHash="ngModel">
        </div>

        <div class="input-group">
            <label for="confirmPassword">Confirm Password</label>
            <input 
                type="password" 
                id="confirmPassword" 
                name="confirmPassword" 
                [(ngModel)]="candidateRegistrationDto.confirmPassword"
                placeholder="Confirm your password" 
                required
                #confirmPassword="ngModel">
        </div>

        <div class="input-group">
            <label for="phoneNumber">Phone Number</label>
            <input 
                type="tel" 
                id="phoneNumber" 
                name="phoneNumber" 
                [(ngModel)]="candidateRegistrationDto.phoneNumber"
                pattern="[0-9]{10}" 
                placeholder="e.g., 9876543210" 
                required
                #phoneNumber="ngModel">
        </div>

        <div class="input-group">
            <label for="candidateRole">Previous Role</label>
            <input 
                type="text" 
                id="candidateRole" 
                name="candidateRole" 
                [(ngModel)]="candidateRegistrationDto.candidateRole"
                placeholder="Enter your previous role" 
                required
                #candidateRole="ngModel">
        </div>

        <button 
            type="submit" 
            class="register-btn" 
            id="registerBtn"
            [disabled]="isLoading"
            [class.loading]="isLoading">
            <span *ngIf="!isLoading">Create Account</span>
            <span *ngIf="isLoading">Creating Account...</span>
        </button>

        <div class="login-link">
            <p>Already have an account? <a routerLink="/dashboard/candidate-login">Login here</a></p>
        </div>
    </form>
</div>
